net:
  isbg:
    currency:
      collector:
        fixer_io_api_endpoint: https://data.fixer.io/api
        fixer_io_api_key: your_api_key_here
        fixer_io_mock: true
        refresh_interval: 5000
spring:
  main:
    keep-alive: true
logging:
  level:
    net:
      isbg:
        currency: DEBUG